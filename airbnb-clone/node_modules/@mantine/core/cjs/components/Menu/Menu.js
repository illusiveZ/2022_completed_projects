'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var hooks = require('@mantine/hooks');
var ActionIcon = require('../ActionIcon/ActionIcon.js');
var Popper = require('../Popper/Popper.js');
var MenuIcon = require('./MenuIcon.js');
var MenuBody = require('./MenuBody/MenuBody.js');
var MenuBody_styles = require('./MenuBody/MenuBody.styles.js');
var Menu_styles = require('./Menu.styles.js');
var useMantineTheme = require('../../theme/use-mantine-theme/use-mantine-theme.js');
var mergeStyles = require('../../theme/utils/merge-styles/merge-styles.js');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e['default'] : e; }

var React__default = /*#__PURE__*/_interopDefaultLegacy(React);

var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b));
var __objRest = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
const MENU_SIZES = MenuBody_styles.sizes;
const defaultControl = /* @__PURE__ */ React__default.createElement(ActionIcon.ActionIcon, null, /* @__PURE__ */ React__default.createElement(MenuIcon.MenuIcon, null));
function Menu(_a) {
  var _b = _a, {
    control = defaultControl,
    children,
    onClose,
    onOpen,
    opened,
    themeOverride,
    style,
    menuId,
    menuBodyProps = {},
    closeOnItemClick = true,
    transitionDuration = 250,
    size = "md",
    shadow = "md",
    position = "bottom",
    placement = "start",
    gutter = 5,
    withArrow = false,
    transition = "pop-top-left",
    transitionTimingFunction,
    menuButtonLabel,
    controlRefProp = "elementRef",
    trigger = "click",
    radius = "sm",
    delay = 0,
    zIndex = 1,
    elementRef,
    classNames,
    styles,
    closeOnScroll = true,
    trapFocus = true,
    onMouseLeave,
    onMouseEnter,
    onChange
  } = _b, others = __objRest(_b, [
    "control",
    "children",
    "onClose",
    "onOpen",
    "opened",
    "themeOverride",
    "style",
    "menuId",
    "menuBodyProps",
    "closeOnItemClick",
    "transitionDuration",
    "size",
    "shadow",
    "position",
    "placement",
    "gutter",
    "withArrow",
    "transition",
    "transitionTimingFunction",
    "menuButtonLabel",
    "controlRefProp",
    "trigger",
    "radius",
    "delay",
    "zIndex",
    "elementRef",
    "classNames",
    "styles",
    "closeOnScroll",
    "trapFocus",
    "onMouseLeave",
    "onMouseEnter",
    "onChange"
  ]);
  const theme = useMantineTheme.useMantineTheme(themeOverride);
  const classes = Menu_styles['default']({ theme }, classNames, "menu");
  const _styles = mergeStyles.mergeStyles(classes, styles);
  const controlRefFocusTimeout = React.useRef();
  const delayTimeout = React.useRef();
  const [referenceElement, setReferenceElement] = React.useState(null);
  const _transitionDuration = hooks.useReducedMotion() ? 0 : transitionDuration;
  const uuid = hooks.useId(menuId);
  const [_opened, setOpened] = hooks.useUncontrolled({
    value: opened,
    defaultValue: false,
    finalValue: false,
    rule: (val) => typeof val === "boolean",
    onChange: (value) => value ? typeof onOpen === "function" && onOpen() : typeof onClose === "function" && onClose()
  });
  const openedRef = React.useRef(_opened);
  const handleClose = (scroll = false) => {
    if (openedRef.current) {
      openedRef.current = false;
      setOpened(false);
      if (trigger === "click") {
        controlRefFocusTimeout.current = window.setTimeout(() => {
          !scroll && typeof (referenceElement == null ? void 0 : referenceElement.focus) === "function" && referenceElement.focus();
        }, transitionDuration + 10);
      }
    }
  };
  const handleOpen = () => {
    openedRef.current = true;
    setOpened(true);
    window.clearTimeout(controlRefFocusTimeout.current);
  };
  hooks.useWindowEvent("scroll", () => closeOnScroll && handleClose(true));
  const wrapperRef = hooks.useClickOutside(() => _opened && handleClose());
  const toggleMenu = () => {
    _opened ? handleClose() : handleOpen();
  };
  const controlEventHandlers = trigger === "click" ? { onClick: toggleMenu } : { onMouseEnter: handleOpen, onFocus: handleOpen };
  const handleMouseLeave = (event) => {
    typeof onMouseLeave === "function" && onMouseLeave(event);
    if (trigger === "hover") {
      if (delay > 0) {
        delayTimeout.current = window.setTimeout(() => handleClose(true), delay);
      } else {
        handleClose(true);
      }
    }
  };
  const handleMouseEnter = (event) => {
    typeof onMouseEnter === "function" && onMouseEnter(event);
    window.clearTimeout(delayTimeout.current);
  };
  const menuControl = React.cloneElement(control, __spreadProps(__spreadValues({}, controlEventHandlers), {
    role: "button",
    "aria-haspopup": "menu",
    "aria-expanded": _opened,
    "aria-controls": uuid,
    "aria-label": menuButtonLabel,
    title: menuButtonLabel,
    [controlRefProp]: hooks.useMergedRef(setReferenceElement, elementRef)
  }));
  return /* @__PURE__ */ React__default.createElement("div", __spreadValues({
    ref: wrapperRef,
    style: __spreadValues({ display: "inline-block", position: "relative" }, style),
    onMouseLeave: handleMouseLeave,
    onMouseEnter: handleMouseEnter
  }, others), menuControl, /* @__PURE__ */ React__default.createElement(Popper.Popper, {
    referenceElement,
    transitionDuration: _transitionDuration,
    transitionTimingFunction,
    transition,
    mounted: _opened,
    position,
    placement,
    gutter,
    withArrow,
    arrowSize: 3,
    zIndex,
    arrowClassName: classes.arrow,
    arrowStyle: _styles.arrow
  }, /* @__PURE__ */ React__default.createElement(MenuBody.MenuBody, __spreadProps(__spreadValues({}, menuBodyProps), {
    opened: _opened,
    onClose: handleClose,
    id: uuid,
    themeOverride,
    closeOnItemClick,
    size,
    shadow,
    classNames,
    styles,
    radius,
    trapFocus: trigger !== "hover" && trapFocus,
    transitionDuration: _transitionDuration
  }), children)));
}
Menu.displayName = "@mantine/core/Menu";

exports.MenuBody = MenuBody.MenuBody;
exports.MENU_SIZES = MENU_SIZES;
exports.Menu = Menu;
//# sourceMappingURL=Menu.js.map
