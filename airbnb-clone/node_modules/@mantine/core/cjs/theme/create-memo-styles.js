'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var reactJss = require('react-jss');
var mergeClassNames = require('./utils/merge-class-names/merge-class-names.js');

function createMemoStyles(styles) {
  const useStyles = reactJss.createUseStyles(styles);
  return function useMemoStyles(args, classNames, name) {
    const validArgs = typeof args === "object" && args !== null;
    const dependencies = validArgs ? Object.keys(args).filter((key) => key !== "theme").map((key) => args[key]) : [];
    if (validArgs && "theme" in args) {
      dependencies.push(args.theme.colorScheme);
    }
    const stylesProps = React.useMemo(() => args, dependencies);
    return mergeClassNames.mergeClassNames(useStyles(stylesProps), classNames, name);
  };
}

exports.createMemoStyles = createMemoStyles;
//# sourceMappingURL=create-memo-styles.js.map
