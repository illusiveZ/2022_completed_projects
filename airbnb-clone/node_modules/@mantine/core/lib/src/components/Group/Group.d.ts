import React from 'react';
import { DefaultProps, MantineNumberSize } from '../../theme';
import useStyles, { GroupPosition } from './Group.styles';
export declare type GroupStylesNames = keyof ReturnType<typeof useStyles>;
export interface GroupProps extends DefaultProps<GroupStylesNames>, React.ComponentPropsWithoutRef<'div'> {
    /** Defines justify-content property */
    position?: GroupPosition;
    /** Defined flex-wrap property */
    noWrap?: boolean;
    /** Defines flex-grow property for each element, true -> 1, false -> 0 */
    grow?: boolean;
    /** Space between elements */
    spacing?: MantineNumberSize;
    /** Defines flex-direction property, row for horizontal, column for vertical */
    direction?: 'row' | 'column';
    /** Defines align-items css property */
    align?: React.CSSProperties['alignItems'];
    /** Defines padding for the root component */
    withGutter?: boolean;
}
export type { GroupPosition };
export declare function Group({ className, style, position, align, children, noWrap, grow, spacing, direction, withGutter, themeOverride, classNames, styles, ...others }: GroupProps): JSX.Element;
export declare namespace Group {
    var displayName: string;
}
//# sourceMappingURL=Group.d.ts.map