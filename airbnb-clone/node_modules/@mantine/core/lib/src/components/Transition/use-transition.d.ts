import { MantineThemeOverride } from '../../theme';
export declare type TransitionStatus = 'entered' | 'exited' | 'entering' | 'exiting' | 'pre-exiting' | 'pre-entering';
interface UseTransition {
    duration: number;
    timingFunction: string;
    mounted: boolean;
    themeOverride: MantineThemeOverride;
    onEnter?(): void;
    onExit?(): void;
    onEntered?(): void;
    onExited?(): void;
}
export declare function useTransition({ duration, timingFunction, mounted, onEnter, onExit, onEntered, onExited, themeOverride, }: UseTransition): {
    transitionDuration: number;
    transitionStatus: TransitionStatus;
    transitionTimingFunction: string;
};
export {};
//# sourceMappingURL=use-transition.d.ts.map