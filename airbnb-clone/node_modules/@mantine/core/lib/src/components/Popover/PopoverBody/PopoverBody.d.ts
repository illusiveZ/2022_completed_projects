import React from 'react';
import { DefaultProps, MantineNumberSize } from '../../../theme';
import useStyles from './PopoverBody.styles';
export declare type PopoverBodyStylesNames = keyof ReturnType<typeof useStyles>;
interface PopoverBodyProps extends DefaultProps<PopoverBodyStylesNames>, React.ComponentPropsWithoutRef<'div'> {
    shadow: string;
    radius: MantineNumberSize;
    spacing: MantineNumberSize;
    elementRef: React.ForwardedRef<HTMLDivElement>;
    withCloseButton: boolean;
    titleId: string;
    bodyId: string;
    onClose(): void;
    closeButtonLabel: string;
}
export declare function PopoverBody({ classNames, styles, themeOverride, shadow, spacing, radius, elementRef, withCloseButton, title, titleId, bodyId, onClose, closeButtonLabel, children, ...others }: PopoverBodyProps): JSX.Element;
export declare namespace PopoverBody {
    var displayName: string;
}
export {};
//# sourceMappingURL=PopoverBody.d.ts.map