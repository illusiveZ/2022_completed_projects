import React from 'react';
import { DefaultProps, MantineSize, MantineNumberSize } from '../../../theme';
import useStyles from './DefaultValue.styles';
export declare type DefaultValueStylesNames = keyof ReturnType<typeof useStyles>;
interface DefaultValueProps extends DefaultProps<DefaultValueStylesNames>, React.ComponentPropsWithoutRef<'div'> {
    label: string;
    onRemove(): void;
    disabled: boolean;
    size: MantineSize;
    radius: MantineNumberSize;
}
export declare function DefaultValue({ label, classNames, styles, className, style, onRemove, themeOverride, disabled, size, radius, ...others }: DefaultValueProps): JSX.Element;
export declare namespace DefaultValue {
    var displayName: string;
}
export {};
//# sourceMappingURL=DefaultValue.d.ts.map