import React from 'react';
import { DefaultProps, MantineSize } from '../../theme';
import { ColorSliderStylesNames } from './ColorSlider/ColorSlider';
import { HueSlider, HueSliderProps } from './HueSlider/HueSlider';
import { AlphaSlider, AlphaSliderProps } from './AlphaSlider/AlphaSlider';
import { SaturationStylesNames } from './Saturation/Saturation';
import { SwatchesStylesNames } from './Swatches/Swatches';
import { ThumbStylesNames } from './Thumb/Thumb';
import useStyles from './ColorPicker.styles';
export { HueSlider, AlphaSlider };
export type { HueSliderProps, AlphaSliderProps };
export declare type ColorPickerStylesNames = keyof ReturnType<typeof useStyles> | ColorSliderStylesNames | SwatchesStylesNames | SaturationStylesNames | ThumbStylesNames;
export interface ColorPickerBaseProps {
    /** Controlled component value */
    value?: string;
    /** Uncontrolled component default value */
    defaultValue?: string;
    /** Called when color changes */
    onChange?(color: string): void;
    /** Color format */
    format?: 'hex' | 'rgba' | 'rgb' | 'hsl' | 'hsla';
    /** Set to false to display swatches only */
    withPicker?: boolean;
    /** Predefined colors */
    swatches?: string[];
    /** Number of swatches displayed in one row */
    swatchesPerRow?: number;
    /** Predefined component size */
    size?: MantineSize;
}
export interface ColorPickerProps extends DefaultProps<ColorPickerStylesNames>, ColorPickerBaseProps, Omit<React.ComponentPropsWithoutRef<'div'>, 'onChange' | 'value' | 'defaultValue'> {
    /** Force picker to take 100% width of its container */
    fullWidth?: boolean;
    /** Should interactive elements be focusable */
    focusable?: boolean;
    /** Static selector base */
    __staticSelector?: string;
    /** Saturation slider aria-label */
    saturationLabel?: string;
    /** Hue slider aria-label */
    hueLabel?: string;
    /** Alpha slider aria-label */
    alphaLabel?: string;
}
export declare function ColorPicker({ value, defaultValue, onChange, format, swatches, swatchesPerRow, size, withPicker, fullWidth, focusable, __staticSelector, saturationLabel, hueLabel, alphaLabel, themeOverride, className, style, styles, classNames, ...others }: ColorPickerProps): JSX.Element;
export declare namespace ColorPicker {
    var displayName: string;
}
//# sourceMappingURL=ColorPicker.d.ts.map