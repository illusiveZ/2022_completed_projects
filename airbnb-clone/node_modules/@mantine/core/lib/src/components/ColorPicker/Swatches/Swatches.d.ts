import React from 'react';
import { DefaultProps } from '../../../theme';
import { HsvaColor } from '../types';
import useStyles from './Swatches.styles';
export declare type SwatchesStylesNames = keyof ReturnType<typeof useStyles>;
interface SwatchesProps extends DefaultProps<SwatchesStylesNames>, Omit<React.ComponentPropsWithoutRef<'div'>, 'onSelect'> {
    data: string[];
    onSelect(color: HsvaColor): void;
    swatchesPerRow?: number;
    focusable?: boolean;
    __staticSelector?: string;
}
export declare function Swatches({ data, onSelect, swatchesPerRow, focusable, classNames, styles, style, __staticSelector, ...others }: SwatchesProps): JSX.Element;
export declare namespace Swatches {
    var displayName: string;
}
export {};
//# sourceMappingURL=Swatches.d.ts.map