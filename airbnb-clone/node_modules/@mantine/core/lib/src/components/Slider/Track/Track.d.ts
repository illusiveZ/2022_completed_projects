import React from 'react';
import { DefaultProps, MantineNumberSize } from '../../../theme';
import { MarksStylesNames } from '../Marks/Marks';
import useStyles from './Track.styles';
export declare type TrackStylesNames = keyof ReturnType<typeof useStyles> | MarksStylesNames;
interface TrackProps extends DefaultProps<TrackStylesNames> {
    filled: number;
    offset: number;
    marks: {
        value: number;
        label?: React.ReactNode;
    }[];
    size: MantineNumberSize;
    radius: MantineNumberSize;
    color: string;
    min: number;
    max: number;
    value: number;
    children: React.ReactNode;
    onChange(value: number): void;
}
export declare function Track({ filled, size, color, classNames, styles, radius, themeOverride, children, offset, ...others }: TrackProps): JSX.Element;
export declare namespace Track {
    var displayName: string;
}
export {};
//# sourceMappingURL=Track.d.ts.map