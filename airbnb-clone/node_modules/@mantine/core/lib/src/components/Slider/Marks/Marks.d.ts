import React from 'react';
import { DefaultProps, MantineNumberSize } from '../../../theme';
import useStyles from './Marks.styles';
export declare type MarksStylesNames = keyof ReturnType<typeof useStyles>;
interface MarksProps extends DefaultProps<MarksStylesNames> {
    marks: {
        value: number;
        label?: React.ReactNode;
    }[];
    size: MantineNumberSize;
    color: string;
    min: number;
    max: number;
    value: number;
    onChange(value: number): void;
    offset?: number;
}
export declare function Marks({ marks, color, size, min, max, value, themeOverride, classNames, styles, offset, onChange, }: MarksProps): JSX.Element;
export declare namespace Marks {
    var displayName: string;
}
export {};
//# sourceMappingURL=Marks.d.ts.map