import React from 'react';
import { DefaultProps } from '../../theme';
import useStyles from './Blockquote.styles';
export declare type BlockquoteStylesNames = keyof ReturnType<typeof useStyles>;
export interface BlockquoteProps extends DefaultProps<BlockquoteStylesNames>, Omit<React.ComponentPropsWithoutRef<'blockquote'>, 'cite'> {
    /** Border and icon color from theme */
    color?: string;
    /** Icon, defaults to quote icon */
    icon?: React.ReactNode;
    /** Describe a reference to a cited quote */
    cite?: React.ReactNode;
}
export declare function Blockquote({ className, style, color, icon, cite, children, themeOverride, classNames, styles, ...others }: BlockquoteProps): JSX.Element;
export declare namespace Blockquote {
    var displayName: string;
}
//# sourceMappingURL=Blockquote.d.ts.map