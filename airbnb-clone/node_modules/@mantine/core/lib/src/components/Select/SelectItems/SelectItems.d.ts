import React from 'react';
import { DefaultProps, MantineSize } from '../../../theme';
import { SelectItem } from '../types';
import useStyles from './SelectItems.styles';
export declare type SelectItemsStylesNames = keyof ReturnType<typeof useStyles>;
interface SelectItemsProps extends DefaultProps<SelectItemsStylesNames> {
    data: SelectItem[];
    hovered: number;
    __staticSelector: string;
    isItemSelected?(itemValue: string): boolean;
    uuid: string;
    itemsRefs?: React.MutableRefObject<Record<string, HTMLDivElement>>;
    onItemHover(index: number): void;
    onItemSelect(item: SelectItem): void;
    size: MantineSize;
    itemComponent: React.FC<any>;
    nothingFound?: React.ReactNode;
}
export declare function SelectItems({ data, hovered, themeOverride, classNames, styles, isItemSelected, uuid, __staticSelector, onItemHover, onItemSelect, itemsRefs, itemComponent: Item, size, nothingFound, }: SelectItemsProps): JSX.Element;
export declare namespace SelectItems {
    var displayName: string;
}
export {};
//# sourceMappingURL=SelectItems.d.ts.map