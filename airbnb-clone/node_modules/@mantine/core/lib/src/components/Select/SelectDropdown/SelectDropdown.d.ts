import React from 'react';
import { DefaultProps } from '../../../theme';
import { MantineTransition } from '../../Transition/Transition';
import useStyles from './SelectDropdown.styles';
export declare type SelectDropdownStylesNames = keyof ReturnType<typeof useStyles>;
interface SelectDropdownProps extends DefaultProps<SelectDropdownStylesNames> {
    mounted: boolean;
    transition: MantineTransition;
    transitionDuration: number;
    transitionTimingFunction: string;
    uuid: string;
    shadow: string;
    maxDropdownHeight?: number | string;
    children: React.ReactNode;
    __staticSelector: string;
    elementRef?: React.ForwardedRef<HTMLDivElement>;
}
export declare function SelectDropdown({ themeOverride, mounted, transition, transitionDuration, transitionTimingFunction, uuid, shadow, maxDropdownHeight, children, classNames, styles, elementRef, __staticSelector, }: SelectDropdownProps): JSX.Element;
export declare namespace SelectDropdown {
    var displayName: string;
}
export {};
//# sourceMappingURL=SelectDropdown.d.ts.map