import React from 'react';
import { ComponentPassThrough } from '../../../types';
import { DefaultProps, MantineNumberSize } from '../../../theme';
import { MenuItemProps } from '../MenuItem/MenuItem';
import useStyles from './MenuButton.styles';
export declare type MenuButtonStylesNames = keyof ReturnType<typeof useStyles>;
export interface MenuButtonProps extends DefaultProps<MenuButtonStylesNames>, MenuItemProps {
    children: React.ReactNode;
    hovered: boolean;
    onHover(): void;
    elementRef(node: HTMLButtonElement): void;
    component?: any;
    radius?: MantineNumberSize;
}
export declare function MenuButton<T extends React.ElementType = 'button'>({ className, style, children, onHover, hovered, themeOverride, elementRef, icon, color, disabled, rightSection, component: Element, classNames, styles, radius, ...others }: ComponentPassThrough<T, MenuButtonProps>): JSX.Element;
export declare namespace MenuButton {
    var displayName: string;
}
//# sourceMappingURL=MenuButton.d.ts.map