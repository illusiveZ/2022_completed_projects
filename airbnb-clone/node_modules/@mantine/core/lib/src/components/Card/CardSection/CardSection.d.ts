import React from 'react';
import { DefaultProps, MantineNumberSize } from '../../../theme';
export interface CardSectionProps extends DefaultProps, React.ComponentPropsWithoutRef<'div'> {
    padding?: MantineNumberSize;
}
export declare function CardSection({ className, themeOverride, padding, ...others }: CardSectionProps): JSX.Element;
export declare namespace CardSection {
    var displayName: string;
}
//# sourceMappingURL=CardSection.d.ts.map