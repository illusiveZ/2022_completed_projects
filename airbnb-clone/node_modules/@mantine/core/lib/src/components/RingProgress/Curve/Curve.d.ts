/// <reference types="react" />
import { MantineThemeOverride } from '../../../theme';
interface CurveProps {
    value?: number;
    size: number;
    offset: number;
    sum: number;
    thickness: number;
    root?: boolean;
    color?: string;
    themeOverride?: MantineThemeOverride;
}
export declare function Curve({ size, value, offset, sum, thickness, root, color, themeOverride, }: CurveProps): JSX.Element;
export declare namespace Curve {
    var displayName: string;
}
export {};
//# sourceMappingURL=Curve.d.ts.map